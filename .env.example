# No variables needed for MVP if using SQLite defaults,
# but providing one for deployment overrides (e.g. Render)
DATABASE_URL=sqlite:///./news.db

# Optional override (default is Periódico Cubano)
RSS_FEED_URL=https://www.periodicocubano.com/feed/
